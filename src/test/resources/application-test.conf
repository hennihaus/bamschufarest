ktor {
    deployment {
        port = 8080
    }
    application {
        timezone = Europe/Berlin
        apiVersion = v1
    }
    cors {
        allowedProtocol = http
        allowedHost = "localhost:4200"
    }
}